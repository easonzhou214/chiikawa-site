---
name: chiikawa-archive-guide
description: 查询《吉伊卡哇》剧情、角色设定与经历、集数、台词、篇章或桥段时使用；普通闲聊和技术任务不触发。
---

# 吉伊卡哇资料库

首次使用先读本说明并调用 `describe_schema`；本会话已读取的说明和已核实的信息可复用。

## 查询

- 已知 id：直接 `get_entry`；集用集号，短篇用「集号:序号」（从 1 开始），角色/篇章用其 id。
- 找桥段或未知 id：`search_entries`，先限制 `limit`，按命中的 `ref` 取详情。
- 查角色或篇章关联集：`get_related`，用 `episode_ids`；可读列表 `episodes` 受 `limit` 限制。
- 浏览目录才用 `list_entries`；按 `has_more` 和 `offset` 分页，仅需完整目录时取全量。
- `get_entry(compact=true)` 精简输出；需要台词转述或具体动作时再取完整条目。检索片段不是全文。

## 解释结果

- 原始 `characters[].eps` 只是代表性出场；服务返回声明与台词/动作倒推的并集，仍可能漏掉无台词的出场，不能称为全集。
- 篇章主体区间与集的篇章链接（含呼应/回访）含义不同；服务已取并集，核对返回的 `coverage`。
- 短篇 `confidence`（原始 `sh[].c`）：A 有资料支撑，B 依据标题和设定整理；B 不应当作已证实的剧情或经历。
- 资料是文字转述，非官方文案或逐字字幕；没有画面、时间轴、播放量或评分。第 241–345 集中文标题为整理者拟定。
- 查不到就说明未知，不用常识补全；优先检索标题与台词，不自行读取工程源码拼凑剧情。
